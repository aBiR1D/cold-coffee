name: cold_coffee
version: '1.0.0'
config-version: 2

profile: cold_coffee

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cold_coffee:
    +materialized: view
    staging:
      +materialized: view
